// Development policy for the Online Edition.
// Payment enforcement stays OFF while synchronized play is being field-tested.
window.TBU_EDITION = Object.freeze({
  name: 'online',
  hostingAccess: 'development-open',
  invitedGuestsJoinFree: true,
  synchronizedGameplay: true
});
